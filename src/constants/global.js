export const CONFIG = {
  golden: '#947c22',
}

export const COLORS = {
  PRIMARY: '#A58C57'
}

const sizes = {
  mobile : '40em',
  laptop : '1440px'
}

export const device = {
  mobile: `(max-width : ${sizes.mobile})`,
  laptop: `(max-width : ${sizes.laptop})`
}