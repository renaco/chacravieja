import styled from "styled-components"
import { CONFIG } from "./../../constants/global"

export const SubTitleContact = styled.span`
  color: ${CONFIG.golden};
`
export const TitleContacts = styled.p`
  display: flex;
`